<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Approval">

<select id="getName" parameterType="approval">
<![CDATA[
select t.emp_name as "",a.emp_name as "mgr_name" from
(select emp_name from emp where emp_no = (select emp_mgr from emp where emp_no=#{emp_no})) t,
(select emp_name from emp where job_no=3) a;

]]>



</select>




</mapper>